<!DOCTYPE html>
<html lang="es">
    <head>
        <%- include("partials/head-base.ejs", {title: "Crear cuenta"}) %>
        <link rel="stylesheet" href="/css/login-register-create.css" />
    </head>

    <body>
        <header><%- include("partials/header.ejs") %></header>

        <main>
            <% if (typeof errors != 'undefined'){ %>
            <ul>
                <% for (i=0; i < errors.length; i++) {%>
                <li style="color: aliceblue;"><%= errors[i].msg %></li>
                <% } %>
            </ul>
            <% } %>
            <section class="register-back">
                <div class="register">
                    <h2>CREAR CUENTA</h2>
                    <form
                        action="/users/register"
                        method="POST"
                        enctype="multipart/form-data"
                        id="registerForm"
                    >
                        <input
                            id="name"
                            type="text"
                            placeholder="Nombre y Apellido"
                            name="name"
                            class=""
                        />
                        <input type="text" placeholder="Usuario" name="user" class=""/>
                        <input type="email" placeholder="Email" name="email" class=""/>
                        <input
                            type="text"
                            placeholder="Domicilio"
                            name="address"
                            class=""
                        />
                        <input
                            type="password"
                            placeholder="Contraseña"
                            name="password"
                            class=""
                        />
                        <input
                            type="password"
                            placeholder="Repetir contraseña"
                            name="password2"
                            class=""
                        />
                        <input type="file" class="userAvatar" name="avatar" />
                        <button type="submit">REGISTRARME</button>
                    </form>
                </div>
            </section>
        </main>

        <footer><%- include("partials/footer.ejs") %></footer>
    </body>
</html>
